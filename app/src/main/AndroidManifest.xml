<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.tugraz.flatshareapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".OverviewActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".CreateFlatFormActivity">
        </activity>
        <activity android:name=".RoommatesActivity" />
        <activity android:name=".ShoppingListActivity" />
        <activity android:name=".CleaningScheduleActivity" />
        <activity android:name=".FlatListActivity" />
        <activity android:name=".FinancingFurnitureActivity" />
        <activity android:name=".BillsActivity" />
    </application>

</manifest>